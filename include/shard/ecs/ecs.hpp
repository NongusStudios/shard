#pragma once

#include <queue>
#include <array>

#include "../renderer/renderer2d.hpp"
#include "system.hpp"

namespace shard{
    template<typename T, size_t size>
    class ComponentArray{
        public:
            
    };

    class ECS{
        public:

        private:

    };
} // namespace shard
